window.onload = function() {

    // var layer1 = ArmContext.Layer({name: "layer1", container: "container"});

    // A = new ArmContext.Rect({layer: layer1, width : 150, height: 150});

    // A.Rotate({x: 75, y: 75, gradAngle: 45}).Translate({x:100, y:100}).Scale({x: 1, y: 0.5}).Draw();

    Root = new ArmGraph.Root({});
    console.log(Root);

    O1 = new ArmGraph.ArmObject( );    
    console.log(O1);


};